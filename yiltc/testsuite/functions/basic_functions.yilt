fn main()
    let a = add(3, 4)
    let b = mul(5, 6)
    let c = greet("world")

fn add(x, y)
    return x + y

fn mul(x, y)
    return x * y

fn greet(name)
    print("hello " + name)
    return name
